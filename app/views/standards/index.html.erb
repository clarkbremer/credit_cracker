<p id="notice"><%= notice %></p>

<h1>Standards</h1>

<table id='standards_index_table' class='dtable cell-border table table-striped table-bordered' cellspacing='0' width='100%'>
  <thead>
    <tr>
      <th>Subject</th>
      <th>Code</th>
      <th>Benchmark</th>
      <th>Example</th>
      <th class="w-25"></th>
    </tr>
  </thead>

  <tbody>
    <% @standards.each do |standard| %>
      <tr>
        <td><%= standard.subject %></td>
        <td><%= standard.code %></td>
        <td class='small'><%= standard.benchmark %></td>
        <td class='small'><%= standard.example %></td>
        <td><%= link_to 'Show', standard, class: "btn btn-primary btn-xs" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add New Standard', new_standard_path, class: "glyphicon glyphicon-plus" %>

<script type="text/javascript">
$(document).ready(function(){
  $('#standards_index_table').DataTable({
    stateSave: true,
    "pageLength": 15,
    "lengthMenu": [ [10, 15, 25, 50, -1], [10, 15, 25, 50, "All"] ],
    "order": [[ 0, "asc" ], [1, "asc"]],
    "columnDefs": [
      { "targets": [2,3,4], "orderable": false},
      { "targets": [1], "orderData": [0,1]}
    ]
  });
});
</script>
