<p>
  <strong>User:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Roles:</strong>
  <%= @user.roles.pluck(:name).join(', ') %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
